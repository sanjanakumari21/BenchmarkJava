name: Java CI with Maven

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'zulu'
#    - name: print java_home
#      run: echo "JAVA_HOME=$JAVA_HOME">>$GITHUB_ENV
    - name: Run Spotless check
      run: mvn spotless:check
    - name: Setup Snyk to check for vulnerabilities
      uses: snyk/actions/setup@master
    - name: run snyk
      run: |
       echo $JAVA_HOME
       mvn --version
       java --version
       docker pull snyk/snyk:maven
       docker run snyk/snyk:maven --workdir /github/workspace "JAVA_HOME" -e "SNYK_TOKEN" -e "snyk" "test"
       docker ps
#       snyk test
      env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}      
